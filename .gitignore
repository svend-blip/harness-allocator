# Byte-compiled / optimized / cache
__pycache__/
*.py[cod]
*$py.class

# Test / build artifacts
.pytest_cache/
.venv/
venv/
build/
dist/
*.egg-info/

# Secrets / local config (never committed)
.env
*.local.toml

# Databases, env variants, backup/editor litter (added 2026-09-01)
*.db
*.sqlite*
*.db-wal
*.db-shm
.env.*
!.env.example
*.bak
*.old
*.orig
*~
*.swp
*.pid
.claude/settings.local.json
.mypy_cache/
.ruff_cache/
.coverage
.DS_Store

# Machine-local app config — harness-allocator.ini.example is the
# committed template; copy it into place per repo README (added 2026-09-01)
harness-allocator.ini
